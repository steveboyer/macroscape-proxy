version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    groups:
      aws-sdk:
        patterns:
          - '@aws-sdk/*'
      aws-cdk:
        patterns:
          - 'aws-cdk-lib'
          - 'constructs'
      dev-dependencies:
        dependency-type: development
        update-types:
          - minor
          - patch
    ignore:
      # Prettier 3.9.x never converges on issues.md; see the TypeScript 6 / prettier pin commit.
      - dependency-name: prettier
        versions: ['>=3.9.0']
      # typescript-eslint (even canary) declares peer typescript '<6.1.0', so TypeScript 7 breaks
      # `npm ci` with ERESOLVE. Drop this once typescript-eslint supports TS 7.
      - dependency-name: typescript
        versions: ['>=7.0.0']
